# 複製這個檔案為 .env，填入實際的值
# .env 不會進 git（已加入 .gitignore）

# JWT 密鑰（至少 32 個字元）
JWT_SECRET_KEY=dev-secret-key-must-be-at-least-32-chars!!

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Grafana（選填，預設值為 admin）
GRAFANA_PASSWORD=admin
